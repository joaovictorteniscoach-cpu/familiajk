name: P0 validation temporary
on:
  push:
    branches:
      - security/p0-aluno-identity-20260924
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run full checks
        run: bash ferramentas/checar-tudo.sh
